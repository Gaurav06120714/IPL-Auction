{
  "name": "ipl-fantasy-auction",
  "version": "1.0.0",
  "description": "Real-time IPL Fantasy Auction - run both frontend and backend together",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "db:push": "npm run db:push --prefix backend",
    "sync:players": "node scripts/sync-player-photos.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
